PORT=5000
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/mitram
JWT_SECRET=replace_with_secure_jwt_secret
CORS_ORIGIN=*
NODE_ENV=development

# Create a real .env locally and do NOT commit it. Example:
# cp .env.example .env